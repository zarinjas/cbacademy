# Apache snippet: enable X-Sendfile for protected video delivery.
# Ensure mod_xsendfile is installed and enabled.

<IfModule mod_xsendfile.c>
    # Allow xsendfile to send files from the storage videos directory
    XSendFile on
    XSendFilePath /full/path/to/your/project/storage/app/videos
</IfModule>
